


<ol>


  <button class="category-work" data-id="{{id}}" data-availability="{{availability}}" class="btn waves-effect waves-light"
    type="submit">
    <p id="workNow">
      <i class="material-icons right">send</i>
      {{this.name}}'s Profile
    </p>
  </button>

  {{!--
  <button class="delete-category" data-id="{{this.id}}" class="btn waves-effect waves-light" type="submit" id="">DELETE!
    <i class="material-icons">delete</i>
  </button> --}}
</ol>

{{!-- <div class="row">
  <div class="col s12 m3">
    <div class="card">
      <div class="card-image">
        <img src="./assets/images/worker.jpeg">
        <span id="workerName" class="card-title">{{this.name}}</span>
        <a href="/category" class="btn-floating halfway-fab waves-effect waves-light green">
          <i class="material-icons">add</i>
        </a>
      </div>
      <div class="card-content" class="change-available" data-id="{{id}}" data-avail="{{sleep}}">
        <p>name: {{this.name}}</p>
        <p>zip_code: {{zip_code}}</p>
        <p>service: {{service}}</p>
        <hr> 
      </div>
    </div>
  </div>
</div> --}}